from pydantic import BaseModel
from typing import Optional

class CourseQuery(BaseModel):
    # TODO: Making sure what UI/UX would like to query in this API
    # Note that since we should have some situation that we
    pass